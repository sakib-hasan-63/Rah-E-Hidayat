# Optional: Base URL for backend API.
# Leave blank when deployed as a unified fullstack app on Vercel (same origin /api).
# Only set this if your backend API is hosted on a separate external server (e.g. Render, Railway).
VITE_API_URL=
